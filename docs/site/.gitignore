# Zensical build output
/site/

# Local Python environment for the docs toolchain
/.venv/

.cache

# Python bytecode from build_llms.py and its tests. (.pytest_cache/ ignores
# itself via a .gitignore pytest writes inside it.)
__pycache__/